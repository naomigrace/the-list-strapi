{
  "routes": [
    {
      "method": "GET",
      "path": "/vacations",
      "handler": "vacations.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/vacations/count",
      "handler": "vacations.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/vacations/:id",
      "handler": "vacations.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/vacations",
      "handler": "vacations.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/vacations/:id",
      "handler": "vacations.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/vacations/:id",
      "handler": "vacations.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
